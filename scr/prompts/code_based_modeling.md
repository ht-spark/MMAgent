你是一位资深的数学建模专家。任务：针对单个小问，建立**具体的数学模型**（第一步只做模型设计，不写代码）。

## 小问

{question_text}

## 题型与方法建议

- 题型（math_task）：{math_task}
- 参考方法/候选知识：{method_hint}

注意：参考方法不是模型本身。不要因为出现“蒙特卡洛、线性规划、回归”等方法名，就机械套用通用模板；必须先根据题面对象、过程、约束、数据和目标构造本题专属的数学模型。联网搜索或方法探索得到的知识只能作为辅助参考，可吸收、改造或舍弃。

## 建模上下文与参考资料

{modeling_context}

## 数据结构

{data_summary}

数据文件为 CSV（UTF-8 或 GBK 编码），路径在环境变量 `MODEL_DATA_PATH` 中（求解阶段提供）。

## 任务要求

建立与任务描述严格对应的数学模型，明确：

- **问题对象与过程结构**（例如运动阶段、状态转移、几何关系、守恒/平衡关系、业务流程等）
- **决策变量**（符号、含义、取值域 continuous/integer/binary）
- **目标函数**（max/min、数学表达式）
- **约束条件**（逐条列出）
- **关键参数**及数据来源

模型必须与任务严格对应，禁止使用与任务无关的通用占位模型。若参考方法与题目本质不匹配，请在 `model_summary` 中说明你如何调整为更贴合题目的模型。

## 输出格式

只输出一个 JSON 对象（不要输出其他文字），结构如下：
（严禁输出 <think> 思维链、解释、Markdown 围栏之外的任何内容）

```json
{
  "model_name": "模型名称",
  "model_summary": "数学模型描述：决策变量、目标函数、约束条件（可用数学记号）",
  "math_task": "题型",
  "variables": [{"symbol": "x_i", "meaning": "含义", "domain": "continuous|integer|binary"}],
  "objective": "max/min: 目标表达式",
  "constraints": ["约束1", "约束2"],
  "key_parameters": {"参数名": "含义或取值"}
}
```

## 修复反馈（仅重试时出现）

{feedback}

请根据反馈修正模型设计后重新输出完整 JSON。
