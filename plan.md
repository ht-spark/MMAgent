# 数学建模智能体执行计划

> 本计划以 [architecture.md](D:\MMAgent\architecture.md) 的“全局上下文 + 逐问闭环 + 最终交付”架构为准。
>
> 实施策略：先完成一条可端到端运行的最小闭环，再逐步扩展题型、工具与审查能力。不要先搭建复杂多层子图。

---

## 1. MVP 范围

第一个可用版本必须能够处理：一份题目文档、多个 CSV/Excel 附件（含多个 Sheet）、两个以上递进小问，并输出 Markdown 论文草稿和完整的运行产物。

MVP 优先支持三类高频建模任务：

1. 多指标评价与排序：熵权法、TOPSIS、层次分析等。
2. 预测与回归：描述统计、线性/多元回归、基础时间序列。
3. 优化决策：线性规划、整数规划和基础动态规划。

暂不作为 MVP 阻塞项：复杂深度学习、自动多智能体协商、全自动 Word 排版、多供应商搜索并发、复杂沙箱修复和强制人工审批。

---

## 2. 目标目录结构

```text
math_modeling_agent/
  main.py
  graph.py
  state.py
  config.py

  schemas/
    common.py
    context.py
    data.py
    question.py
    evidence.py
    result.py
    paper.py

  workflow/
    intake.py
    project_context.py
    question_loop.py
    final_review.py
    delivery.py

  agents/
    problem_understanding.py
    method_research.py
    question_solver.py
    result_writer.py
    paper_writer.py
    reviewer.py

  tools/
    document_tools.py
    data_profile_tools.py
    preprocess_tools.py
    modeling_tools.py
    validation_tools.py
    plotting_tools.py

  runtime/
    artifacts.py
    checkpoint.py
    budget.py
    logging.py

  prompts/
  tests/
  examples/
```

目录按职责划分：`workflow/` 只负责流程，`agents/` 负责结构化推理，`tools/` 负责确定性计算，`schemas/` 负责数据契约。不要让 Agent 直接读写任意文件或直接生成最终数值。

---

## 3. 分阶段执行计划

### Phase 0：项目基线与数据契约

**目标**：建立可运行的项目骨架、配置、日志、产物目录和核心 Schema。

**任务**：

1. 初始化 Python 项目、依赖、配置和测试环境。
2. 定义 `ProjectContext`、`DataProfile`、`QuestionResult`、`EvidenceItem`、`ArtifactRecord`、`ReviewIssue` 等 Pydantic Schema。
3. 实现运行 ID、产物目录、结构化日志和检查点接口。
4. 定义错误分类与预算对象：输入错误、数据错误、方法错误、计算错误、验证错误、写作错误。
5. 建立样例题目录，至少包含一份多 Sheet Excel 文件和一道两问以上的题目。

**验收**：

- 任何工作流节点都能接收和返回结构化 Schema。
- 一个运行可以生成独立的 `artifacts/<run_id>/` 目录。
- 缺少配置、文件不存在或 Schema 不合法时返回可读错误，而非静默失败。

### Phase 1：输入摄入与全局上下文

**目标**：输入题目和附件后，得到可靠的题目理解、数据画像、小问列表和依赖关系。

**任务**：

1. 实现题目文档解析，支持 Markdown、TXT、DOCX/PDF 的文本提取接口。
2. 实现 CSV、Excel、JSON 读取；Excel 必须遍历所有工作簿和 Sheet。
3. 生成数据画像：行列数、字段类型、描述统计、缺失率、重复率、异常值线索、时间字段、单位线索和表间关联候选。
4. 实现题目理解 Agent：背景、目标、约束、全部小问、预期输出和歧义点。
5. 实现小问拆分与依赖识别；为每问生成题型初判和题目-数据映射。
6. 实现 `G0` 输入质量门及失败路径。

**测试**：

- 多 Sheet Excel 的每个 Sheet 都出现在画像中。
- 缺失列、空表、不可读文件和重复字段能够被记录。
- 显式小问不会漏提，依赖图不存在循环。
- 文件或题干不完整时，工作流进入澄清状态而不是继续建模。

**验收**：

```text
题目 + 附件
  -> ProjectContext
  -> DataProfile
  -> questions / dependencies / question_data_map
```

### Phase 2：逐问闭环骨架

**目标**：先不追求复杂方法，让系统能够按顺序遍历小问、继承必要上下文并保存结果包。

**任务**：

1. 实现项目编排器：根据依赖关系选择当前可执行的小问。
2. 实现 `CurrentQuestionContext` 装配器：注入当前题、相关数据、全局约束和前问 `reusable_summary`。
3. 实现选择性继承规则，禁止直接传递完整历史推理。
4. 实现小问状态机：`pending -> solving -> validating -> validated`，并支持 `blocked`。
5. 实现 `QuestionResult` 的读写、版本记录和 `GQ` 小问结果门。
6. 实现局部回退机制：重做 Q2 时不影响已经验证的 Q1。

**测试**：

- 两问递进题中，Q2 能读取 Q1 的可复用结论。
- Q2 不会接收到 Q1 的无关中间记录。
- Q2 验证失败时，Q1 的结果包与产物不变。
- 无依赖问题可被标识为可并行，但 MVP 默认仍按顺序执行。

**验收**：

系统可以使用 Stub 求解器跑完多个小问，并为每问生成合法的 `QuestionResult`。

### Phase 3：方法探索与决策

**目标**：让当前小问在数据和题意约束下，形成可审计的模型选择，而不是直接跳到代码生成。

**任务**：

1. 建立题型到候选方法的规则库，先覆盖评价、预测、优化三类。
2. 实现方法研究 Agent：识别当前小问的知识缺口，按需检索方法前提、领域知识、参数范围和验证方式。
3. 实现 `EvidenceItem` 与来源登记；每条关键外部事实和方法依据必须关联来源与局限。
4. 为每个候选方法生成“适用条件、数据要求、假设、输出、优缺点、验证方法、放弃条件”。
5. 实现硬过滤：数据不满足、假设不成立、无法回答题目或不可验证的方案直接淘汰。
6. 实现可解释的比较评分，并保存主方法、基线/备用方法和选择理由。
7. 对关键假设不明、候选全部失效或研究预算耗尽的情况实现人工介入请求。

**测试**：

- 无时间字段时，时间序列候选会被过滤。
- 小样本场景不会把高参数模型作为默认主方法。
- 线性目标与约束的优化题能优先给出线性规划候选。
- 复杂模型未体现可解释增益时，基线方案仍被保留。
- 无来源的网络断言不能进入正式论文引用库。

**验收**：

每个小问在计算前都有完整 `decision_record`，并且能说明“为何选它、为何不用其他方案、将如何验证”。

### Phase 4：数据处理、建模计算与可视化

**目标**：将选定方法转换为可运行、可复现的计算产物。

**任务**：

1. 实现按小问的数据准备计划：字段选择、类型转换、缺失处理、异常处理、标准化、指标方向配置和数据划分。
2. 原始文件只读，处理后数据和配置单独保存。
3. 实现 MVP 模型工具：熵权法/TOPSIS、线性回归、线性或整数规划；动态规划作为优化类的扩展工具。
4. 实现统一的计算执行接口，记录代码、参数、输入数据版本、随机种子、日志、表格和图片。
5. 实现基础可视化工具：数据分布图、评价排序图、拟合与残差图、优化结果图、敏感性图。
6. 实现计算错误的定向重试，区分数据、语法、求解器和逻辑错误。

**测试**：

- 模型工具对已知小样例给出正确结果。
- 熵权法权重和为 1，TOPSIS 结果范围正确。
- 回归误差和残差由实际计算生成。
- 优化工具能区分最优、无解和无界。
- 相同输入、参数和随机种子能重复得到相同结果。

**验收**：

每个已完成小问均有可复跑的代码、结果表和至少一张能够支撑结论的图。

### Phase 5：题型验证与结果沉淀

**目标**：确保计算结果真正可信，并把每问整理成后续写作和递进求解可直接使用的结果包。

**任务**：

1. 实现通用验证：单位和量级、边界、约束、数据泄漏、数值重跑和结论覆盖检查。
2. 实现评价模型验证：指标方向、权重扰动、排名稳定性、替代方法对比。
3. 实现预测模型验证：训练/测试指标、残差、基线比较和稳健性检查。
4. 实现优化模型验证：可行性、目标值、边界情形和参数敏感性。
5. 为每问生成结果叙述：主要发现、图表解读、现实含义、限制、最终结论和可复用摘要。
6. 实现验证失败的局部回退与重选模型边界。

**测试**：

- 约束被违反时不能通过验证。
- 排名对小幅权重扰动高度敏感时会产生风险提示。
- 测试误差显著恶化时会阻止过度结论。
- 验证失败只会重跑当前小问的受影响步骤。

**验收**：

只有通过 `GQ` 的结果才能进入后续小问和论文写作；风险无法消除时必须在 `limitations` 中明确记录。

### Phase 6：全题审查与论文写作

**目标**：从已验证结果包生成符合建模竞赛逻辑的论文，并避免写作阶段产生不可追溯内容。

**任务**：

1. 实现全题审查：小问覆盖、前后依赖、符号与单位一致性、数据口径、图表和数值可追溯性、验证充分性。
2. 实现论文大纲与分节写作，固定为问题重述、分析、假设、符号、数据处理、分问建模与求解、检验、评价、参考文献和摘要。
3. 将每个小问的 `QuestionResult` 映射为相应论文章节。
4. 实现公式、图、表编号和正文引用检查。
5. 实现引用登记：外部事实对应 `EvidenceItem`，数值对应计算产物。
6. 最后生成摘要，不允许摘要引入未在正文验证过的新数字或结论。
7. 实现格式与内容审查，并将问题定位到具体章节或小问。

**测试**：

- 小问缺章节、图片未引用、表格未解释、引用不存在都会报错。
- 论文中的关键数字无法定位到计算结果时不能通过。
- 摘要遗漏关键方法或结论时会被标记。
- 审查问题只回退到对应章节或小问。

**验收**：

系统输出结构完整的 Markdown 论文草稿、引用库、审查报告和提交清单。

### Phase 7：端到端加固与扩展

**目标**：在真实竞赛题上验证流程稳定性，再扩展能力。

**任务**：

1. 用至少三套样例端到端测试：评价题、预测题、优化题，其中至少一套为多问递进题。
2. 记录每个阶段的耗时、失败率、人工介入次数和产物完整率。
3. 加入恢复运行、缓存复用、预算控制和失败报告。
4. 根据真实失败案例补充题型规则、验证器和提示词，而不是先扩张 Agent 数量。
5. 在 MVP 稳定后扩展：更多优化算法、聚类、仿真、Word/PDF 输出、更多检索源和受控并行。

**验收**：

- 三类样例均能从输入运行到论文草稿和审查报告。
- 单题失败不会破坏其他已验证结果。
- 恢复运行不会无故重复已完成的确定性计算。
- 关键产物缺失、验证失败或预算耗尽时都有可读的报告。

---

## 4. 建议开发顺序

建议严格按以下顺序推进：

```text
Phase 0 -> Phase 1 -> Phase 2 -> Phase 4 -> Phase 5 -> Phase 6
                         |                         ^
                         +------ Phase 3 ----------+
                                      |
                                      v
                                   Phase 7
```

说明：Phase 3 的方法探索可以先使用规则库和少量受控检索实现；Phase 4 的确定性工具应尽早落地，因为它决定系统能否生成可信数值。复杂联网、多 Agent 并发和自动代码修复属于后续增强，不应阻塞 MVP。

---

## 5. 每次迭代的完成定义

每完成一个功能，至少同时交付：

1. 对应 Schema 与状态更新规则。
2. 正常路径和失败路径的自动化测试。
3. 可检查的中间产物或报告。
4. 与 `architecture.md` 一致的验收样例。
5. 不破坏已完成小问的局部回退行为。

以下情况不视为完成：只有提示词没有确定性产物、只有代码没有验证、只有论文文本没有结果溯源、只能跑单个 CSV 而无法处理多 Sheet 附件，或某问失败导致全流程从头开始。

---

## 6. 近期首个开发任务

先实现 **Phase 0 与 Phase 1**：输入摄入、全 Sheet 数据画像、题目拆问、依赖关系和题目-数据映射。这是后续逐问建模能否可靠运行的基础，也最容易用真实样例验证。
