## Phase 1：输入与上下文（G0 门控）

```
START → intake → context → [G0 门控]
```

| 节点 | 职责 | 实现 |
|---|---|---|
| `intake` | 读取任务文档+附件，生成确定性数据画像（行列/类型/缺失/分布/时空） | `workflow/intake.py` |
| `context` | LLM 三步：understand（提取背景/目标/约束）→ decompose（拆小问/建依赖图）→ classify（题型分类） | `workflow/project_context.py` |
| `g0_retry` | 消耗 INTAKE_RETRY 预算，重跑 intake | `graph.py` |
| `g0_clarification` | 硬失败暂停，等用户终止或上传补充材料 | `graph.py` |

G0 路由：`pass` → 逐问求解 / `retry` → intake / `human` → clarification

## Phase 2-5：逐问求解闭环（GQ 门控）

```
select_question → assemble_context → configure_budget → solve_question → validate_result → [GQ 门控] → archive_result → select_question(循环)
```

| 节点 | 职责 |
|---|---|
| `select_question` | 按依赖图选下一个可执行小问，全部完成则跳 Phase 6 |
| `assemble_context` | 装配当前小问上下文：目标、数据映射、前序问题继承摘要 |
| `configure_question_budget` | 可选：用户临时覆盖该问的检索/候选/修复预算上限 |
| `solve_question` | 核心求解：方法探索 → 建模 → 代码执行 → 结果生成 |
| `validate_result` | 题型验证（评价/预测/优化等不同题型不同验证策略） |
| `gq_check` | GQ 质量门：pass → 归档 / retry → 重解 / blocked → 归档跳过 |
| `archive_result` | 归档结果，循环回 `select_question` |

## Phase 6：全局审查与交付（GF 门控）

```
global_review → write_paper → review_paper → [GF 门控] → deliver → END
                          ↑                          |
                          └── gf_revise (修订) ──────┘
```

| 节点 | 职责 |
|---|---|
| `global_review` | 全任务一致性审查（跨问题矛盾/遗漏检查） |
| `write_paper` | 报告写作：结构化章节 + LaTeX 公式 + 图表引用 |
| `review_paper` | 报告审查：critical/major/minor 问题分级 |
| `gf_revise` | 消耗 PAPER_REVISION 预算，带审查反馈回到 write_paper |
| `deliver` | 保存 Markdown + 转 DOCX + 审查报告 JSON → END |

## 三个门控的定位

| 门控 | 位置 | 硬失败 | 软失败 | 预算类型 |
|---|---|---|---|---|
| **G0** | context 之后 | 上下文缺失/问题空/依赖环/题干空 | 目标空/输出空/fallback | `INTAKE_RETRY` |
| **GQ** | 每问求解之后 | 结果缺失/计算未复现 | 图表不全/解释不足 | `VALIDATION_ITERATION` |
| **GF** | 报告审查之后 | critical 问题未修复 | major 问题 | `PAPER_REVISION` |

