# 2026-07-27 工作日志

## 构建文件读取与数据画像工具（file_tools）

按 architecture.md / plan.md 的六步工作流，完成了第一个确定性 Tool：

### 产出文件
- `scr/schemas/problem.py` — DataField / DataInventory / NumericStats / CategoricalStats 等 Pydantic Schema
- `scr/schemas/__init__.py` — 导出 Schema
- `scr/tools/file_tools.py` — CSV/Excel/Markdown 读取 + generate_data_inventory 数据画像
- `scr/tools/__init__.py` — tools 包初始化
- `scr/__init__.py` — scr 根包初始化
- `tests/conftest.py` — pytest 全局配置 + 测试夹具（CSV/Excel/MD 动态生成）
- `tests/unit/tools/test_file_tools.py` — 45 个单元测试，全部通过
- `pyproject.toml` — 新增 openpyxl 依赖 + pytest 配置（pythonpath, testpaths, asyncio_mode）

### 关键设计决策
1. 文件名 `file._tools.py` 含点号无法被 Python 导入，新建 `file_tools.py` 实现，旧文件保留为迁移说明
2. 项目实际目录是 `scr/`（非 `src/`），Schema 和 tools 是同级包，用相对导入 `from ..schemas.problem import`
3. CSV 读取默认 utf-8，失败回退 gbk（中文 Windows 兼容）
4. 数据画像包含：行列数、字段类型推断、缺失率、单位线索、时间维度检测、数值/分类统计
5. 时间列检测三重规则：字段名关键词 → pandas datetime 类型 → 字符串 to_datetime 试解析
6. 单位线索从字段名括号中正则提取（货币/百分比/重量/长度/时长/温度/频率/功率）

### 测试覆盖（45 passed）
- 文件读取：CSV/Excel/Markdown 正常 + FileNotFoundError + 编码回退 + 不支持类型
- 数据画像：维度/缺失率/时间检测/单位线索/数值统计/分类统计/列分类/示例值/唯一值
- 边界情况：空 CSV / 小样本(<30) / 字符串数字 / 全空列 / 常量列 / JSON 产物可还原 / Excel 画像

## 构建 ProblemAnalyst 子智能体（L0 understand/decompose/classify）

按 architecture.md §4 L0 和 plan.md Phase 3.3，构建了第一个 LLM Agent：

### 产出文件
- `scr/schemas/problem.py` — 扩展：ProblemAnalysis / SubProblem / SubProblemList / ProblemClassification
- `scr/schemas/__init__.py` — 导出新 Schema
- `scr/agents/__init__.py` — agents 包初始化
- `scr/agents/base.py` — BaseAgent 基类（prompt 加载 + LLM 调用 + 结构化输出）
- `scr/agents/problem_analyst.py` — ProblemAnalyst（understand/decompose/classify/analyze）
- `scr/math_modeling_agent/config.py` — LLMConfig 配置类
- `scr/prompts/problem_analysis.md` — understand 提示词模板
- `scr/prompts/task_decomposition.md` — decompose 提示词模板
- `scr/prompts/problem_classification.md` — classify 提示词模板
- `tests/unit/agents/test_problem_analyst.py` — 19 个单元测试，全部通过

### 关键设计决策
1. BaseAgent 支持 LLM 注入：测试传 FakeLLM，生产从环境变量创建 ChatOpenAI
2. prompt 模板用 {var} 占位符，_render_prompt 逐个替换，不影响 JSON 示例中的 {}
3. decompose 返回 list[SubProblem]，通过 SubProblemList 包装类适配 with_structured_output
4. 测试不需要 langchain 安装——FakeLLM 实现 with_structured_output + invoke 接口即可
5. Prompt 与代码分离：模板存 prompts/*.md，Agent 加载后渲染

### 全部测试统计
- 64 passed（45 file_tools + 19 problem_analyst）
